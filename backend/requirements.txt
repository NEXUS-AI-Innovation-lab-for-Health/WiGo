# Versions épinglées sur celles réellement validées en exécution : un build
# doit produire deux fois le même environnement.
fastapi==0.128.8
uvicorn==0.39.0
sqlalchemy==2.0.49
psycopg2-binary==2.9.12
pydantic==2.13.4
python-multipart==0.0.20
requests==2.32.5
pyvips==3.1.1
minio==7.2.20
numpy==2.0.2

# Client HTTP du micro-service InstanSeg. Le backend ne fait QUE des appels
# réseau vers lui : `instanseg-torch` a été retiré d'ici, il n'était importé
# nulle part et embarquait tout PyTorch (image de 15 Go).
httpx==0.28.1

# Migrations de schéma. `create_all` crée les tables absentes mais n'ajoute
# jamais de colonne à une table existante : sans Alembic, toute évolution
# casse silencieusement les bases déjà déployées.
alembic==1.16.5
